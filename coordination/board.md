# Run log — topological-phases-of-matter

Append-only. Newest entries at the bottom. One entry per agent action of record.

---

**2026-07-14 — agent: kb-builder**

Produced the deep-research knowledge base seeding the six-paper series.

- Read `context/source.txt` in full and extracted the program (condensed moduli stack
  `Ham_{d,G}` → uniformly gapped substack `Gap_{d,G}` → stabilized phase ∞-groupoid
  `Phase_{d,G}` → invertible condensed phase spectrum `IP^cond_{d,G}`), the transition
  picture (`U_f`, `Σ_f`, `ν_f`, relative charge `∂ν ∈ E^{q+1}(B,U_f)`), the SSH worked
  example, Aoki's solid-K-theory bridge, and the profinite disorder hull `Ω = F^{Z^d}`.
- Deep web research: **49 sources verified** (title + authors + arXiv-ID/DOI each confirmed
  against arxiv.org / journals via WebSearch/WebFetch). **0 dropped** — no source made the
  list without confirmation. Bibliography floor (40) cleared.
- Wrote:
  - `.knowledge-base.md` — program overview + one section per topic (Papers I–V + synthesis),
    each with known results & citations, provable-Theorem vs open-Conjecture candidates,
    techniques, pitfalls; plus topic×source cross-reference matrix.
  - `references.bib` — 49 verified entries (each with a `% verified:` line) + 6 canonical
    companion-paper entries (`paperLocality … paperSynthesis`).
  - `coordination/notation.md` — canonical symbol table + copy-verbatim LaTeX macro block.
  - `coordination/board.md` — this log.
- Novelty (for the novelty reviewer): condensed mathematics (Clausen–Scholze; Barwick–Haine
  pyknotic) and its K-theory bridge (Aoki, arXiv:2409.01462) are established in pure math;
  the *moduli/space-of-states-of-gapped-systems* viewpoint is established in physics
  (Hsin–Wang 2211.16535; Beaudry et al 2303.07431; Kubota 2503.12618; De Nittis(-Rendel)
  2502.03590 / 2607.01183). **No prior work applies condensed/pyknotic machinery to
  topological phases.** The synthesis is novel; individual analytic ingredients are not.
- Warnings for workers (see `.knowledge-base.md` for detail):
  1. **Paper III** must respect Cubitt–Pérez-García–Wolf undecidability (arXiv:1502.04573):
     no claim of a general algorithm/uniform criterion deciding the gap; stability results
     are conditional (LTQO / frustration-free hypotheses).
  2. **Paper V** must respect Kapustin–Fidkowski (arXiv:1810.07756): chiral phases (nonzero
     Hall conductance) are NOT realizable by local commuting projector Hamiltonians —
     realizability claims must exclude this obstruction class.
  3. Everything beyond cited today's-theorems is a numbered Conjecture, not a Theorem.

---

**2026-07-14 — worker: positivity-cstar-norms (Part II)** — COMPLETE. "Positivity, C*-Norms, and Condensed State Spaces of Quasi-Local Algebras" (`paperPositivity`, math.OA). 20 pp / 1353 src lines; Theorems II-A (condensed state space), II-B (Aoki solidification bridge, stated with real-Banach + connective/periodic hypotheses), II-C (Bellissard crossed-product disorder algebra), plus condensed-C*/GNS-functor/ground-state-continuity props and free-fermion/SSH section; Conjectures II-1…II-5 (closed condensed substack, solid-invariant naturality in the hull, Real/KKO refinement, solid state-space classifying object, split-property/superselection). Disorder alphabet written Q (not F) to avoid clash with Part I's F-function. Gemini (agy/gemini-3.1-pro): 2 rounds → MINOR REVISIONS, all items fixed. Codex: 3 rounds → PASS (fixed cleveref sibling names via thmtools, duplicate hyperref anchors, overfull/needspace/mbox, ~\cite, \frenchspacing). pdflatex clean: 0 overfull / 0 underfull / 0 dup-dest / 0 undefined. Haskell src/positivity-cstar-norms/ (5 modules: Core, GNS, Properties, Proofs, Main) compiles -Wall -Wextra -Werror, runs exit 0, 6/6 QuickCheck + 9/9 deterministic checks. Cover images/positivity-cstar-norms.png (2550×3300). Coordination abstract posted.

**2026-07-14 — worker-eft (Part IV `lattice-eft-equivalence`)** — "From Lattice Models to Effective Field Theories: Stabilization and the Invertible Condensed Phase Spectrum": 23-page PDF (1358 src lines), 4 Haskell modules under GHC 9.10.1 (Main exits 0; 21 QuickCheck properties pass — monoid laws, Grothendieck universal property, tenfold-way Bott periodicity). Gemini peer review 3 rounds → ACCEPT. Codex formatting 3 rounds (fixed: aliascnt theorem naming, texorpdfstring headings, bold-extra, flafter, 2 doubled-space-before-cite; round-3 NEEDS_FIX is environmental only — read-only sandbox, fresh build verified in Stage 7). Theorems from cited results; lattice↔EFT equivalence kept as organizing Conjecture IV-2 (never claimed proven). Artifacts: papers/pdf/lattice-eft-equivalence.pdf, images/lattice-eft-equivalence.png, src/lattice-eft-equivalence/, coordination/abstracts/lattice-eft-equivalence.md.

**2026-07-14 — worker: positivity-cstar-norms (Part II)** — PANEL FIXES APPLIED. MAJOR: restated Theorem II-B to Aoki's actual Theorem A — solidification of *connective* algebraic K-theory recovers connective semitopological K-theory (Friedlander–Walker, and Blanc); operator K-theory is the Bott-inverted periodicization Kop(A) ≃ Solid(Kalg(cond A))[β^{-1}] (β = order-8 real Bott class / K₂(ℂ)≅ℤ complex); propagated through Corollary 6.2 + its proof + abstract + conclusion. Minors: semitopological K-theory attributed to Friedlander–Walker (and Blanc) not Weibel; Thm 4.3 "norm-continuous"→"strongly continuous" family of automorphisms (statement+proof); Thm II-C makes Z^d amenability explicit (full = reduced crossed product); citation-precision — solidification-functor cite now clausen-scholze-condensed + -analytic. Added Code Availability note (github.com/YonedaAI/topological-phases-of-matter, src/positivity-cstar-norms/) and a positivity-cstar-norms.cabal (cabal build OK) for reproducibility parity with Part IV. Codex round 4 (gpt-5.5 xhigh): removed \enlargethispage footer overlap, trimmed abstract to fit page 1. Recompiled: 21 pp, 0 overfull/underfull/dup-dest/undefined; PDF+cover(2550×3300) refreshed.

**2026-07-14 — worker-eft (Part IV `lattice-eft-equivalence`) — panel fixes applied** — GrokRxiv 6-role panel (mostly_sound, 0 critical/0 major): added verified primary citations (Ogata arXiv:2110.04671 for d=1 completeness; Altland–Zirnbauer PRB 55 1142 (1997) at the tenfold-way definition) to both references.bib; reframed the comparison map as conditional on the recognition hypothesis (Conjecture I-1) + un-constructed naturality; softened the d=2 H^3-index claim (index exists, completeness not established); added a Code Availability note; fixed 3 orphan Arbitrary instances in Properties.hs via local newtype wrappers so exe+test compile under -Wall -Wextra -Werror (21/21 QuickCheck still pass). Recompiled: 24pp, 0 errors/overfull/underfull, all 35 cites resolve. PDF + cover regenerated.

---

**2026-07-14 — worker-locality (Part I: `lieb-robinson-locality`)** — COMPLETE. Paper "Condensed Locality: Lieb–Robinson Estimates and Quasi-Local Dynamics on the Moduli Stack of Hamiltonians" — 21 pages (1208 src lines), compiles clean (pdflatex → bibtex → pdflatex×2; 0 errors, 0 overfull, 0 undefined). Theorems I-A/I-B/I-C with full proofs (Banach interaction space + LR dynamics; condensed dynamics morphism; profinite family = compatible finite data + uniform light cone), supporting Props (reweighting, symmetric-derivation generator, naturality over the base, uniform clustering, QAC generation of 𝓦), Conjectures I-1/I-2/I-3. Gemini: 1 round → MINOR REVISIONS (4 minor items fixed). Codex: 2 rounds → PASS (3 findings fixed: everypage→`\AddToHook{shipout/foreground}`, `\cite{paperGap}` doubled-space, braced `\norm`). Haskell src/lieb-robinson-locality/ (5 modules: FFunction, LinAlg, Ising, Properties, Main) compiles `-Wall -Wextra -Werror`, TFIM commutator-norm light cone + velocity fit, 6/6 QuickCheck properties, exits 0. Abstract posted; PDF in papers/pdf/, cover in images/.

**2026-07-14 — worker: bordism-realizability (Part V)** — COMPLETE. Gemini 3 rounds (MINOR→MAJOR→ACCEPT); Codex 2 rounds → PASS (8 formatting fixes). PDF 23pp / 1205 src lines / 34 refs (added verified kitaev-majorana-wire). Haskell src/bordism-realizability/ {Kitaev,Stabilizer,Properties,Main}.hs: -Wall -Wextra -Werror clean, 7/7 QuickCheck, exit 0. Cover image 2550x3300. Kapustin–Fidkowski no-go & Ogata d=1,2 scope honored; Thm V-A/B/C + Conj V-1/2/3.

**2026-07-14 — agent: worker-gap (Part III `spectral-gap-stability`)** — Complete. 25pp PDF; framed around STABILITY (CPW undecidability wall stated as hard limit, never violated); Thms III-A/B/C proved from cited results, Conjectures III-1/2/3 numbered; 3 Haskell modules (TFIM exact diagonalization + Jacobi eigensolver, base-only) with 7/7 QuickCheck properties passing. Gemini 2 rounds → ACCEPT; Codex 3 rounds → PASS (incl. verified aliascnt/cleveref cross-reference fix). Cover image generated. All gates pass.

**2026-07-14 — agent: notation-warden** — Notation consistency audit complete across all 5 papers; wrote `coordination/notation-audit.md`; no paper files edited. Macro-block fidelity, stacking symbol (⊠), phase-label ν/∂ν, and conjecture numbering (I-1..3/II-1..5/III-1..3/IV-1..5/V-1..3 incl. cross-paper citation accuracy) all PASS. 4 BLOCKING findings: (B1) Paper I self-contradicts on the disorder-alphabet letter — bare `\Ffun^{\Z^d}` at line 236 vs. disambiguated `\Ffun_0^{\Z^d}` at lines 894/1066; (B2) Paper III uses undisambiguated bare `F` for the disorder hull while using `\Ffun` 30+ times for the interaction F-function, same glyph, no remark; (B3) `notation.md` itself never recorded Part II's documented F→Q disorder-alphabet deviation, the root cause enabling B1/B2 (Papers IV/V also still use F, advisory A1, non-colliding); (B4) Paper IV's abstract-promised "Theorem IV-B"/"IV-C" tags (Kubota Ω-spectrum, Hall conductance) are absent everywhere in the compiled paper — zero matches for "IV-A/B/C" in the whole file, the only paper in the series missing its headline Roman-letter labels. 5 advisory findings (A1 disorder-letter series drift in IV/V; A2 Paper III's γ vs Δ finite-volume-threshold overlap; A3/A5 three different LaTeX mechanisms for the same Roman-letter theorem/conjecture convention; A4 Paper I's tags present but not restated in prose). Per-paper verdict: I FAIL, II PASS, III FAIL, IV FAIL, V PASS. Minimal fixes proposed per blocking item in the audit doc.

**2026-07-14 — agent: citation-verifier (cross-cutting)** — COMPLETE, all 5 papers PASS, 0 blocking defects. Bib integrity: both references.bib copies byte-identical, 58 entries, 0 duplicate keys, all 52 external entries individually `% verified:`-tagged (6 companion @misc entries covered by a shared header, appropriate for internal self-citations), 1 harmless orphan (`clausen-scholze-complex`, arXiv:2605.11731, never cited). Coverage: all `\cite` keys in all 5 papers resolve at source level (57 unique keys of 58; confirmed also via `pdftotext` scan of all 5 compiled PDFs, 0 `[?]` markers). Gave spectral-gap-stability and bordism-realizability — which had no citation-panel review at all — their first citation-level check; both clean. Externally spot-verified 13 additional entries via WebSearch (exceeding the ≥10 ask), closing every name on the priority list (michalakis-zwolak, nsy-bulkgap, bhm-stability, bravyi-hastings-shortproof, cglw-cohomology, walker-wang, else-nayak, thouless-pump, kitaev-honeycomb, kitaev-majorana-wire, aasen-wang-hastings, ogata-h3-index, kapustin-sopenko-noether) — 0 misattributions found; 35/58 entries (60%) now independently verified overall (22 by prior panels + 13 new). Cross-paper consistency confirmed: companion self-citations fully consistent (each paper cites the other 5, never itself), Clausen–Scholze 2605.xxxxx IDs used consistently with no aliasing. Full report: `coordination/citation-audit.md`.

**2026-07-14 — worker-locality (Part I) — panel fixes applied** — 7 items: (1) corrected TFIM F-norm bound to max{(|h|+2|J|)/F(0), |J|/F(1)} ≤ (2|J|+|h|)/F(1) (diagonal term collects field + 2 incident bonds); (2) cite beaudry-etal as closest non-condensed precedent in intro; (3) added Bratteli–Robinson Vol. 1 (Springer 2nd ed. 1987, ISBN 978-3-540-17093-8) to both references.bib with '% verified:', cited in §2 quasi-local algebra; (4) added src/lieb-robinson-locality/lieb-robinson-locality.cabal (exe + test, QuickCheck, -Wall -Wextra -Werror); (5) replaced nonexistent arXiv-ancillary claim with GitHub Code Availability; (6) fixed §9 velocity-fit prose to the actual relative 10%-of-row-max front threshold; (7) added C_{F_a}≤C_F as a real QuickCheck property (now 7/7, exit 0, -Werror clean). Recompiles clean 21pp (0 undefined, 0 overfull); PDF+cover regenerated.

**2026-07-14 — worker-locality (Part I) — notation B1 fixed** — disorder alphabet moved F/F_0 → Q per notation.md Amendment (Ω = Q^{Z^d}): intro §1.2 (was bare \Ffun^{\Z^d}), Cor. disorder (was \Ffun_0), and Conj. I-3 now all read Q^{\Z^d}; one clarifying remark at first use ("Q denotes the configuration alphabet; F reserved for the F-function"); canonical macro block untouched (byte-identical). Recompiles clean 21pp (0 errors/undefined/overfull); PDF+cover regenerated.

**2026-07-14 — orchestrator** — Notation ruling: the v1 canonical macro-block COMMENT line (`% disorder hull \Omega = F^{Z^d}`) remaining in existing paper preambles is accepted variance (comment-only, non-normative, zero rendering impact); notation.md's block comment was updated to Q so future copies carry the right letter. Macro \dis itself unchanged everywhere. Papers must still use Q in prose/math per the Amendment.

**2026-07-14 — worker: bordism-realizability (Part V)** — PANEL FIXES APPLIED. Downgraded d=2 Ogata completeness to open (Rem V-B', index-exists + surjectivity only; d=1 stays Thm V-B); rewrote Thm V-C as two rigorous parts — electric sigma_H no-go under U(1) (Kapustin–Fidkowski) + thermal c_- no-go (Kapustin–Spodyneiko 1905.06488), removing the false sigma_H=c_- biconditional; E8 non-CP now via c_-/KS not KF; Table 1 attributions split (KF electric / KS chiral) + new fermionic-SPT CP row (Gu–Wen, Tarantino–Fidkowski, Wang–Gu); Hsin–Kobayashi 2505.20384 scope caveat; prop:descent now states "Assuming Conjecture III-2"; added bordism-realizability.cabal + Code-availability line. 5 new verified bib keys (both bibs). Recompiled clean: 25pp, 0 overfull/underfull, 45 refs. Haskell 7/7, exit 0.

**2026-07-14 — worker-eft (Part IV `lattice-eft-equivalence`) — notation B4 fixed** — Tagged the three headline theorems reader-visibly: Theorem IV-A (group-completion universal property, thm:groth), IV-B (Kubota Ω-spectrum, thm:kubota), IV-C (Hall conductance phase invariant, thm:hall), via bold optional-title tags + matching (Theorem IV-A/B/C) mentions in the Introduction; numeric numbering intact. Advisory A1 applied: disorder hull rewritten F^{Z^d} → Q^{Z^d} at all 3 sites (interaction-decay F-function \BF untouched). Recompiled: 24pp, 0 errors/overfull/underfull, all cites/refs resolve; IV-A/B/C tags confirmed in rendered theorem heads. PDF + cover regenerated.

**2026-07-14 — worker: bordism-realizability (Part V)** — ADDENDA 8-10 applied. (8) Thm V-B d=1 completeness now cites ogata-spt-chains (2110.04671) primary + survey secondary [already in place from the V-B rewrite]. (9) Thm V-C proof stripped of the mis-attributed "adiabatic-curvature two-form exact" mechanism (that is Kapustin–Sopenko, not KF) — now cites statements only (KF electric; Kapustin–Spodyneiko thermal). (10) disorder alphabet F->Q per notation.md Amendment 2026-07-14: hull now $\Omega=Q^{\mathbb{Z}^d}$ (abstract, Conj V-3, §9, macro comment). Recompiled clean: 25pp, 0 overfull/underfull, 45 refs. PDF+cover current.

**2026-07-14 — worker-gap (Part III `spectral-gap-stability`) — panel fixes applied.** 8 items: (1) abstract/intro n-gap Lipschitz constant corrected to 2C_Lambda in F-norm (2 is operator-norm); (2) prop:lsc retitled + fallback corrected to lower-semicont gap(Phi)<=liminf; (3) \cite{kapustin-fidkowski} added at both prose mentions; (4) +2 verified refs (bcl-pg-1d-undecidable 1810.01858; castilla-lucia-instability 2607.08686, Jul-2026 instability-of-undecidable-gap) cited near CPW + hsin-wang-moduli precedent in discussion; (5) added spectral-gap-stability.cabal (exe+test, base+QuickCheck, -Werror); (6) trace-identity precision restated (residuals <1e-9 for N<=8, ~1e-13 at N=4); (7) Kramers-Wannier duality now stated in text (ties prop_kwDuality); (8) Code availability line added. Recompile: 26pp, bibtex 0 warnings, 0 overfull, 0 undefined; Haskell 7/7; PDF+cover regenerated.

**2026-07-14 — worker-gap (Part III) — addendum item 9 (notation audit B2) done.** Disorder-hull alphabet switched F->Q series-wide (Ω = Q^{Z^d}) at all 5 sites; \Ffun (F-function, 29 uses) untouched; one clarifying remark added at first use; \dis macro comment synced to notation.md amendment. Recompile 26pp, bibtex 0 warnings, 0 overfull, 0 undefined. All 9 items complete.

**2026-07-14 — worker: bordism-realizability (Part V)** — POLISH (re-verify PASSED, sound, 0 major/critical; KS licensing check passed verbatim). Added Kapustin–Spodyneiko to the Limitations attribution list; reworded Conclusion final sentence to "Kapustin–Fidkowski (electric) and Kapustin–Spodyneiko (thermal) walls". Recompiled clean: 25pp, 0 overfull/underfull. PART V CLOSED.

**2026-07-14 — worker-gap (Part III) — notation B2 fixed (confirmed on disk).** Disorder alphabet F->Q at all 5 hull sites (Ω = Q^{Z^d}; lines 205/325/1180/1187/1460 after the +2 shift from the added remark); grep verified 0 residual F^{...} and 0 disorder-context bare F; \Ffun (29) untouched; clarifying remark at first use. 26pp, bibtex 0 warnings, 0 overfull, 0 undefined. PDF+cover current.

**2026-07-14 — Haskell verifier (Part IV `lattice-eft-equivalence`)** — Proofs.hs created (new test-suite `proofs` component, 18 deterministic checks: Theorem IV-A universal-property witnesses incl. a `mkHom` certified-homomorphism constructor with a genuine rejection test, Example 4.5's K(toy monoid)≅Z derived from the universal property rather than normal-form case analysis, Bott periodicity traced to its algebraic mechanism plus the 5 named Table 1 landmarks). Codex round 1 (gpt-5.5 xhigh): NEEDS_FIX, 7 findings — all fixed: hid the `CMonoid` constructor (only `freeCMonoid`/`toyMonoid` certified) + documented `eqG`'s bounded witness-search honestly; `scale` retyped `Integer`→`Natural` (rejects negative exponents at the type level, zero ripple into `Main.hs`); `freeCMonoid` now stores+validates its generator list via new `respectsAlphabet`; added `mkHom :: CMonoid -> Map String Integer -> Maybe Hom` certified-homomorphism constructor, turning the old ill-defined demo into a real rejection test; fixed 2 stale Haddocks claiming the toy completion "collapses to 0" (paper: K(M)≅Z generated by [t]); added 8 new toy-monoid QuickCheck properties (eqG reflexive/symmetric/transitive/congruence + group laws under `toyMonoid`, not just the cancellative `free3`) and a full 10×8 Table 1 regression matrix. Suite now: Main exit 0 (byte-identical demo output), Properties 31/31, Proofs 18/18, all 3 cabal components `-Wall -Wextra -Werror` clean; `dist-newstyle` cleaned. Liquid Haskell not installed (`which liquid` fails) — skipped. Round-1 review: `reviews/lattice-eft-equivalence-haskell-codex-review-round-1.md`; canonical file pending round 2.

**2026-07-14 — Haskell verifier (Part IV `lattice-eft-equivalence`) — CLOSED, codex PASS.** Round 2 (2 minor findings, both fixed): `Monoid.hs` scale Haddock corrected from "type error" to the accurate "runtime rejection via Natural underflow" (numeric literals are polymorphic, so `scale (-1) x` typechecks and only fails when evaluated); added negative regression `prop_violatesForeignAlphabet` (`not (respectsAlphabet (freeCMonoid ["a"]) (gen "b"))`) alongside the existing positive-path property — Properties 31→32/32. Round 3 (final): VERDICT PASS, zero findings, all round-1/2 fixes independently re-verified in source. Final state, clean rebuild from scratch: all 3 cabal components (`lattice-eft` exe, `properties`/`proofs` test-suites) `-Wall -Wextra -Werror` clean; Main exit 0 (demo output unchanged throughout all 3 rounds); Properties 32/32; Proofs 18/18; `dist-newstyle` and all `.o`/`.hi` artifacts removed. Reviews: round-1/2/3 files plus canonical `reviews/lattice-eft-equivalence-haskell-codex-review.md` (VERDICT: PASS). PART IV HASKELL VERIFICATION CLOSED.

---

**2026-07-14 — worker: synthesis (Part VI `synthesis`)** — COMPLETE. "Topological Phases of Matter in the Condensed-Mathematics Paradigm: A Modular Research Program" (`paperSynthesis`, math-ph, DOI 2026.07.synthesis). 24 pp / 1324 src lines (1161 non-blank). Structured around the boxed spine (local interactions → `\Ham_{d,G}` → observables/solid-K → `\Gap_{d,G}` → `\Phase_{d,G}` → `\IPcond_{d,G}` → realized subspectrum), each arrow owned by a Part; the π-dictionary; the relative-charge transition calculus (LES of the pair, Thom/excision localization, linking spheres — corrected per Gemini) with SSH worked end-to-end through all five modules; the profinite disorder thread `\dis=Q^{Z^d}` (Part II's Q-alphabet convention flagged); modular-composition tikz-cd diagram + emergent-properties argument; both hard walls (CPW undecidability, Kapustin–Fidkowski) respected. 14 Recollections restate the cited module theorems (I-A…V-C, Aoki with Bott-inversion bookkeeping, Kubota, Ogata, Kapustin–Sopenko); program-level claims are numbered Conjectures VI-1 (Master)…VI-4 only. Consolidated open-problems table: 19 module conjectures (I-1…V-3) with dependency + feeds columns → 4 VI bundles. Required "Relation to companion papers" subsection present. Author block Matthew Long / The YonedaAI Collaboration. Gemini (agy/gemini-3.1-pro): 2 rounds → round 1 MINOR REVISIONS (fixed Thom/linking-sphere formula, order-eight→period-eight Bott class, SSH codim-2 origin note) → round 2 ACCEPT. Codex (gpt-5.5 xhigh, run centrally by lead): 3 rounds → r1 NEEDS_FIX (5 findings: starred→numbered VI conjectures, \ref→\Cref, ragged longtable column, addlinespace→midrule, long-heading ToC titles) → r2 NEEDS_FIX (1: longtable page-split infinite-glue → converted to non-breaking table float) → r3 NEEDS_FIX cap (1 cosmetic: float ordering → fixed post-cap with placeins/\FloatBarrier, self-verified). Final compile pdflatex→bibtex→pdflatex×2: 24 pp, 0 errors / 0 overfull / 0 underfull / 0 infinite-glue / 0 float-too-large / 0 undefined / 0 dup-dest, bibtex 0 warnings; all 36 citations resolve (only existing verified references.bib keys used — no new sources needed). Artifacts: papers/pdf/synthesis.pdf, images/synthesis.png (2550×3300), reviews/synthesis-review{,-round-1,-round-2}.md, reviews/synthesis-codex-review{,-round-1,-round-2,-round-3}.md.

**2026-07-14 — orchestrator** — Correction for final Slack summary: the Part I Slack notification quoted "analytic LR bound 5.44"; the Haskell verification phase found that number came from a hardcoded wrong formula. The genuine Proposition 3.2 computation gives v_up = 31.551 (v_fit = 2.564 unchanged; bound still satisfied with more headroom). Paper text quotes no numeric velocity, so no paper edit needed. Final summary should note the corrected bound.

**2026-07-14 — agent: spectral-gap-stability Haskell verification (Part III)** — COMPLETE. src/spectral-gap-stability/ recompiles clean under -Wall -Wextra -Werror, base-only preserved (no array/containers/vector deps); `cabal run sgs-demo` and `cabal test` both exit 0: QuickCheck 7/7, deterministic theorem-instance suite 127/127. Proofs.hs created from scratch (did not exist before this pass): trace identities eq.(17) verified independently of the Jacobi eigensolver; full-spectrum Weyl-inequality instances (\Cref{lem:weyl}); a 3-step replay of Theorem III-A(i)'s own proof (Weyl@0, Weyl@1, triangle inequality) plus the III-A(ii) open-locus corollary; Kramers-Wannier duality (\eqref{eq:disp}, Sec 8.1) proved algebraically and checked at exact dyadic points plus 3 independent boundary anchors (closing the duality check's scale-invariance blind spot); closed-form eigensolver validation independent of the physics model (diagonal 4x4, generic symmetric 2x2) plus TFIM's own degenerate corners (g=0, N=1) and solver-based trace identities at N=6-8 (beyond QuickCheck's N<=5 generator range). Codex (gpt-5.5, xhigh reasoning) 3 rounds: r1 NEEDS_FIX (10 findings, 4 High — unchecked symEigenvalues memory safety, unvalidated Config constructor, unvalidated perturbation-vector length, silent Jacobi non-convergence) → r2 NEEDS_FIX (3 findings — Int-overflow-before-validation in symEigenvalues, maxSites=12 exceeding the paper's own N<=8 validated range, a comment-arithmetic slip) → r3 PASS, zero findings. All fixed: symEigenvalues now `Either String EigenReport` (dimension/overflow/length/finiteness/symmetry-checked, reports residual+sweep count, fails rather than silently returning on non-convergence, Jacobi tolerance now relative to the matrix's own Frobenius norm); Config built only via mkConfig/config smart constructors (nSites in [1,8], finite field, raw constructor no longer exported); hamiltonianWithField validates field-vector length against nSites; gapAt/twoGapAt/perturbedGap return Maybe Double (~10 call sites updated); test-suite split onto its own TestMain.hs (no longer reuses the demo's Main.hs). Gap-table numerics verified byte-identical to 4 decimals before/after every fix; every new validation guard runtime-verified to actually reject bad input (mkConfig/symEigenvalues/hamiltonianWithField), not just type-check. Liquid Haskell not installed on this machine — skipped per instruction. Reviews: reviews/spectral-gap-stability-haskell-codex-review-round-{1,2,3}.md + canonical reviews/spectral-gap-stability-haskell-codex-review.md (VERDICT: PASS). Build artifacts cleaned (cabal clean) after final verification.

**2026-07-14 — humanizer-a — humanizer pass A: papers I-III, 49 edits total.** Prose-only pass (abstract/introduction/discussion/conclusion/section-openers/remark bodies) on lieb-robinson-locality.tex (10 edits), positivity-cstar-norms.tex (16 edits), spectral-gap-stability.tex (23 edits) — spectral-gap-stability had by far the heaviest em-dash tic. Nearly all edits replace a prose em-dash aside with a parenthetical, comma, or colon (per humanizer skill §14); one aphorism-formula fix ("the currency condensation trades in" → "what condensation requires") and one stacked-participle split in Part III's numerics-section opener. Zero theorem/proposition/lemma/conjecture statements, proofs, displayed math, macros, citations, tables, or GrokRxiv sidebar text touched; en-dash compound names (Lieb--Robinson, Nachtergaele--Sims--Young, III-A--III-C ranges) left untouched. Recompiled all 3 (pdflatex→bibtex→pdflatex×2): 0 errors, 0 undefined refs/citations, 0 overfull/underfull, page counts unchanged (21/21/26 pp, matching pre-edit baselines exactly). PDFs copied to papers/pdf/, cover images regenerated at 300dpi, aux files cleaned.

**2026-07-14 — worker: synthesis (Part VI) — panel fixes applied** — BLOCKING restatement-drift fixes after re-reading the CURRENT Part V. (1) rec:VB rewritten: completeness is d=1 ONLY (H^2 bijection); d=2 index exists + surjectivity, completeness OPEN — swept the companion-summary echo too. (2) rec:VC rewritten as the two-part no-go: (i) electric sigma_H=0 under U(1) (Kapustin–Fidkowski) and (ii) thermal c_-=0 no symmetry (Kapustin–Spodyneiko, key kapustin-spodyneiko-thermal now cited); E8 re-routed to the CHIRAL mechanism (sigma_H=0, c_-=8) — swept the Introduction/abstract/walls/SSH/conclusion echoes. (3) §6.3 linking-sphere given its relative-charge-of-defects precedents: thiang-ktheory + teo-kane (Teo–Kane, PRB 82, 115120 (2010), arXiv:1006.0690 — WebSearch-verified, added to BOTH references.bib with '% verified:'); disorder-hull fact attributed to bellissard-ncg-qhe + kellendonk-tilings. (4) Code Availability section added (github.com/YonedaAI/topological-phases-of-matter, src/<topic>/). Recompiled pdflatex→bibtex→pdflatex×2: 25 pp, 0 errors / 0 overfull / 0 infinite-glue / 0 float-too-large / 0 undefined / 0 dup-dest, bibtex 0 warnings (1 benign badness-1102 underfull on the URL line); Table 1 still before §10 (p.20 vs p.21); 40 distinct citations resolve. PDF + cover (2550×3300) regenerated.

**2026-07-14 — worker: synthesis (Part VI) — technical-panel addendum applied** — (6, MAJOR) SSH worked example degree bookkeeping reworked q=1→q=0: the winding NUMBER is a locally-constant Z-label on the parameter plane, nu in K^0(U) (distinct from the momentum-space K^1 tenfold-way home), so with codim c=1 the linking S^0 evaluates in reduced K~^0(S^0)=Z giving the +/-1, matching E^{q-c+1}(pt)=K^0(pt)=Z; rc=∂nu in K^1(B,U). Fixes the prior contradiction (q=1 gave K^1(pt)=0). (7, MINOR) Table 1 bundle count corrected to match the Feeds column: VI-1 ten / VI-2 four / VI-3 one / VI-4 four (10+4+1+4=19), was "nine/three". Rebuild of the current on-disk state (which also carries the concurrent P4.5 humanizer prose edits — file now 1364 lines): 26 pp, 0 errors/overfull/infinite-glue/float-too-large/undefined/dup-dest, bibtex 0 warnings (1 benign badness-1102 underfull on the code-URL line), Table 1 p.21 before §10 p.22, 40 cites resolve. PDF+cover(2550×3300) regenerated. NOTE: compiled the MERGED state — if the humanizer edits further, re-run final compile+cp+cover after P4.5 completes.

**2026-07-14 — humanizer-b — humanizer pass B: papers IV-VI, 75 edits total.** Prose-only pass (abstract/introduction/discussion/conclusion/section-openers/remark bodies) on lattice-eft-equivalence.tex (16 edits), bordism-realizability.tex (23 edits), synthesis.tex (36 edits) — nearly all replace a prose em-dash aside with a parenthetical, comma, or colon (per humanizer skill §14); synthesis.tex had by far the heaviest em-dash tic (a "—X—" aside used almost formulaically throughout), including 4 "Emergent structure" commentary paragraphs treated as remark-equivalent. Zero theorem/proposition/lemma/conjecture/Recollection statements, proofs, displayed math, macros, citations, tables (incl. synthesis Table 1), or the GrokRxiv sidebar touched; en-dash compound names and all technical worked-example passages (SSH walkthrough per-Part paragraphs) left untouched. Two small LaTeX-plumbing fixes alongside the prose (no content/wording change): added `\emergencystretch=2em` to bordism-realizability.tex's preamble (mirrors the sibling papers) after a paren-substitution nudged one line into a 14pt overfull box; scoped `\hbadness=10000` around the Code-Availability `sloppypar` block in both bordism-realizability.tex and synthesis.tex to silence a pre-existing mild underfull on the long-URL line (confirmed via isolated test compiles that the *original* em-dash phrasing had the same or worse underfull badness — not something my edit introduced). Mid-pass, synthesis.tex turned out to be under concurrent edit by worker-synthesis's panel-fix pass (content rewrites to rec:VB/rec:VC, Table-1 bundle-count fix, SSH degree-bookkeeping rework, Code-Availability section) — flagged to team-lead, reconciled by re-reading fresh state, zero content collisions found (their changes and mine never touched the same passages), 3 of my Edit calls failed cleanly against their in-flight text and were retried against the settled state. Final recompiles (pdflatex→bibtex→pdflatex×2) on the fully-merged state: lattice-eft-equivalence 24pp (0 err/overfull/underfull/undefined, unchanged from baseline), bordism-realizability 25pp (0/0/0/0, unchanged), synthesis 26pp (0/0/0/0/0 infinite-glue/float-too-large, Table 1 p.20 before Conclusion p.22 — now §11 after worker-synthesis's additions, was §10; +1pp vs 25pp baseline, within the ±1 tolerance). All 3 PDFs copied to papers/pdf/, covers regenerated at 2550×3300, aux files cleaned.

**2026-07-14 — agent: haskell-verifier (positivity-cstar-norms, Part II)** — COMPLETE, codex round 3 → PASS. src/positivity-cstar-norms/ (Core, GNS, CrossedProduct, Properties, Proofs, Main; 6 modules) compiles `-Wall -Wextra -Werror` clean, exits 0; 15/15 QuickCheck properties, 18/18 deterministic checks; `cabal test` (new test-suite stanza) also passes. Liquid Haskell: `which liquid` → absent, skipped (noted, not attempted). Coverage added beyond the panel-verified baseline (was 6/9): a new `CrossedProduct` module gives Theorem II-C (Bellissard crossed product) its first executable check — a hand-verified finite model (fixed ambient group Z/bigN acting on Z/n via reduction mod n, twisted-convolution product) whose periodization map is proved (on paper, then checked in code) to be a genuine *-homomorphism, the finite-level instance of the theorem's functoriality claim. `GNS.gnsRank` was added to fix a real bug codex caught: the code claimed GNS Hilbert-space dimension is always n², which is false for non-faithful (e.g. pure) states — confirmed the paper itself never echoes this figure, so the fix was Haskell-only. Codex (gpt-5.5, xhigh) ran 3 rounds: round 1 NEEDS_FIX (10 findings — Matrix given full shape-safety via a checked, dimension-carrying type with Either-returning cores; approxEqMat/isHermitian shape-mismatch bug fixed; PD-vs-PSD split made explicit with a diag(1,0) boundary check; II-B marked out-of-executable-scope by design; 6 new QuickCheck properties; cabal test-suite stanza; dependency bounds tightened — caught and correctly resolved a live conflict where the reviewer's suggested explicit `foldl'` import would itself fail -Werror as redundant on the installed GHC 9.14.1/base-4.22); round 2 NEEDS_FIX (2 findings — CrossedProduct.hs's XP type had been missed in round 1's shape-safety pass, fixed identically; a stale module-doc line updated to match the PD/PSD split); round 3 PASS, zero blocking issues. Reviews: reviews/positivity-cstar-norms-haskell-codex-review-round-{1,2,3}.md + canonical reviews/positivity-cstar-norms-haskell-codex-review.md.

**2026-07-14 — worker: synthesis (Part VI) — final build on frozen post-humanizer file** — Re-read the live file after the P4.5 humanizer stop and confirmed all fixes anchored to the final text: item 6 (SSH q=0: nu in K^0(U), ∂nu in K^1(B,U); no stale q=1/K^1(U)/K^2(B,U)), item 7 (Table 1 count 10+4+1+4=19), items 1-2 (rec:VB d=1-complete/d=2-open, rec:VC two-part no-go w/ kapustin-spodyneiko), and teo-kane/thiang/kellendonk citations. Definitive recompile (1365 lines): 26 pp, exit 0, and 0 errors / 0 overfull / 0 UNDERFULL / 0 infinite-glue / 0 float-too-large / 0 undefined / 0 dup-dest, bibtex 0 warnings; sidebar on p1; Table 1 p.21 before §10 p.22; 40 cites resolve. papers/pdf/synthesis.pdf (newer than .tex) + images/synthesis.png (2550×3300) regenerated from the frozen source.

**2026-07-14 — agent: haskell-verifier (lieb-robinson-locality, Part I)** — COMPLETE, codex round 3 → PASS. src/lieb-robinson-locality/ (FFunction, LinAlg, Ising, Properties, Proofs, Main; 6 modules + new Test.hs test-suite driver) compiles `-Wall -Wextra -Werror` clean, both `lr-demo` and `cabal test` exit 0; 8/8 QuickCheck properties (was 7), 12/12 equational-reasoning proof checks (Proofs.hs was new this round, brought to 9 then expanded to 12 across the fix cycle). Liquid Haskell: `which liquid` → absent, skipped (noted, not attempted). Created Proofs.hs from scratch with equational-reasoning proofs each citing a paper theorem/lemma, executable from Main. Codex (gpt-5.5, xhigh) ran 3 rounds directly via background Bash (not codex:rescue, per instruction): round 1 NEEDS_FIX (12 findings, 4 High — missing `foldl'` import in LinAlg.hs; Main.hs's "analytic LR upper bound" was a disconnected `2*e*max(J,h)` formula never routed through `analyticVelocity`, now a genuine Proposition 3.2 computation via a new `analyticUpperBoundTFIM`/`tfimFnormAt` in Proofs.hs, numerically optimized over the reweighting parameter — **this changed the printed v_up from 5.437 to 31.551** (v_fit unchanged at 2.564; confirmed with the paper-owning team lead that no paper text quotes either number, so no paper edit needed); x=0 excluded from the Theorem-3.1-relevant front-position fit plus a new pointwise Theorem 3.1 check against simulated data; Proofs.hs's Lemma 2.4 Z^d claim had been silently reusing 1D-only truncation for d=2,3, replaced with genuine Z^d l1-ball lattice enumeration; plus 7 mediums — separation-parameterized C_Fa test, an honestly-renamed sup-of-ratios property plus a new real interaction-based F-norm property, an I-A/I-B/I-C coverage matrix plus a new uniform-velocity-over-a-parameter-box check (I-C's one computational piece), corrected paper citations throughout (reweight=Lemma 2.3 not 2.4, F-norm=Definition 2.5 not "2.7", velocity=Proposition 3.2 not "2.9", TFIM=Example 3.6/Section 8 not "3.9/Section 9" — verified by exhaustively counting every theorem-environment in the .tex, not sampling), an opaque `FFunction` with clamping smart constructors, matrix well-formedness checking plus `siteOp`/`tfimH` non-positive-length guards, an `opNorm` convergence certificate; 1 low — a suggested cabal `library` stanza caused a real `-Wmissing-home-modules` build failure under this project's flat source layout and was reverted as a documented, scoped exception, keeping only the separate `Test.hs` test-suite driver); round 2 NEEDS_FIX (2 findings — `-Wdodgy-imports` on `import Prelude hiding (foldl')`, legitimate on this project's GHC 9.14.1/base-4.22 toolchain but dodgy on the reviewer's older one, resolved empirically by dropping the import and documenting the base-version boundary; a stale Proofs.hs header paragraph about the citation mismatch rewritten in past tense); round 3 PASS, zero remaining issues. Reviews: reviews/lieb-robinson-locality-haskell-codex-review-round-{1,2,3}.md + canonical reviews/lieb-robinson-locality-haskell-codex-review.md.

**2026-07-14 — lean-formalizer (P5.5 Lean library)** — COMPLETE. `lean/` CondensedPhases lake project, Lean 4.32.0 + Mathlib v4.32.0 (pinned, exact toolchain match, olean cache). 6 modules authored via `codex exec` (gpt-5.5, xhigh): Basic/Interactions/Gap/Stacking/PhaseMonoid/FiveProblems (~614 src lines) + root. 1 codex authoring round-trip; 1 self-fix pass (6-line transparency fix to the Grothendieck `Quotient.lift`/`Quotient.map` congruence, per the mandate's "fix remaining errors yourself"). GATES: `lake build` clean (8662 jobs, 0 errors); `grep sorry`/`axiom` → none; `#print axioms` on all proven items → only [propext, Classical.choice, Quot.sound], no sorryAx. 21 theorems + 2 instances + 1 example + 3 proof-carrying MonoidHoms proven (stacking commutative monoid, min-gap lemmas incl. the trivial-never-lowers-gap guard, full Grothendieck CommGroup with universal property + functoriality). Five central problems stated as Prop-valued defs citing Papers I–V. lean/README.md written (module map + build + statement inventory).

- ✗ OG card generation (some FAIL)

- ✗ OG card generation (some FAIL)

- ✓ OG cards generated (all PASS)

**2026-07-14 — website-builder (P6 Next.js site)** — BUILD COMPLETE, `npm run build` clean, static export to `out/`. Theme `condensed-spectrum` (website/theme.json, written first): indigo-void bg #06060d, ultraviolet accent #7c4dff (condensed moduli-stack register), spectral-cyan accent_secondary #22d3ee (gapless-discriminant/phase-boundary register), amber warning #f0b429 reserved for the invertible capstone only; Space Grotesk/Inter/JetBrains Mono; faint Z^d lattice-dot grid + radial UV glow motif. 7 static routes (landing + 6 papers), 6/6 pages built, capstone (synthesis, Part VI) featured full-width above the 5-module grid. Math: lib/macros.ts transcribes the 24-command canonical block + 7 generic safety-net macros + 110 paper-local macros (11–27/paper) from every papers/latex/*.tex preamble, resolved **per paper slug** (not globally merged) because \id, \Groth, \real genuinely differ across papers (e.g. positivity's \id=\mathbf{1} vs lieb-robinson/synthesis's \id=\mathrm{id}); lib/render-math.ts runs katex.renderToString server-side per math span (he.decode() first — pandoc entity-escapes even the math-span text, e.g. &times;/&#8614;/&middot;) with strict:'ignore', trust:true, throwOnError:true wrapped in try/catch so a failure never surfaces KaTeX's own red error box. Two real gaps found and fixed by actually building against all 6 files rather than assuming: (1) `\begin{tikzcd}` (6 diagrams across 4 papers) has no browser-renderer equivalent at all — given a designed `<details>` fallback ("Commutative diagram — rendered in the PDF" + collapsible raw source) instead of an error; (2) one `\begin{multline*}` (lieb-robinson) isn't supported by KaTeX — rewritten to `\begin{gather*}` (same bare-\\ line syntax, nearest faithful substitution). Final state: 0 katex-error, 0 unintended fallbacks anywhere in `out/`. Also caught via actual build+grep (not assumed): the agent-definition's ref-callback `innerHTML` mount pattern for paper content produced a **completely empty** `<div class="paper-content">` in the static-exported HTML (ref callbacks never fire during SSR/static export — only after client JS mounts), i.e. worse than the "unrendered LaTeX on first load" failure mode it was meant to prevent. Fixed by making PaperContent a plain Server Component with `dangerouslySetInnerHTML` (no 'use client') — since Server Component output is never hydrated/reconciled against a client vdom, the hydration-mangling concern the ref-callback was meant to avoid doesn't apply, and the static HTML now carries the real KaTeX-rendered content (verified: paper-content div went from 0 to ~1MB of real markup; 0 hydration warnings in console). TOC uses scroll-position tracking (last heading with offsetTop <= scrollY+120), not IntersectionObserver, with a desktop sticky sidebar + separate mobile collapsible toggle sharing one component; verified via Playwright at 390px and 1440px (active-section highlighting confirmed mid-scroll on both papers checked). Theorem-like pandoc divs (`class="theorem/proposition/lemma/corollary"`, `definition/example`, `conjecture/progconj`, `recollection`, `remark`, `proof`) get distinct accent-coded left-border treatments matching the series' own "Theorem only if fully proved, else numbered Conjecture" editorial stance — proved statements in violet, conjectures in amber, restated prior results (recollections) in teal. OG metadata: per-page generateMetadata + homepage both set absolute-resolving og:image/og:url/twitter:card via metadataBase (VERCEL_PROJECT_PRODUCTION_URL, falls back to localhost outside Vercel); had to correct the image path to `/og/og-<slug>.png` (og-image-generator's actual output uses an `og-` prefix on every file including per-paper ones, not just `og-default.png` as its own doc's example script showed — matched to what's actually on disk in website/public/og/). Build is self-sufficient: added `scripts/copy-assets.mjs` run via npm `prebuild`/`predev` hooks to vendor docs/papers/*.html → website/content/papers/, papers/pdf/*.pdf and images/*.png → website/public/{pdf,images}/ automatically (gitignored as derived; verified by deleting all three and re-running `npm run build` clean from scratch). Playwright QA at 390px: 0 horizontal overflow on landing + synthesis pages, all 6 cover images load, mobile TOC toggle and prev/next boundary cases (Part I has no "Previous", Part VI is the last "Next") both correct. Known pre-existing limitation, out of this agent's scope: the pandoc HTML's citations (`<span class="citation">`) and some equation cross-references render empty/unresolved (literal `[eq:label]` text) — not introduced by the website build, inherited from the HTML as handed off; flagging for whoever owns the pandoc/citeproc step. Not deployed (per instruction) — orchestrator owns codex review, browser QA, and `vercel --prod`.

**2026-07-14 — agent: browser-QA (P6 browser QA)** — COMPLETE, 0 defects found, 0 fixes applied. Served `website/out/` via `npx serve out -l 4173` (confirmed HTTP 200), tested all 7 routes (`/` + 6 `/papers/<slug>/`) at 390/768/1280px with Playwright MCP. Per-page/per-width results — overflow / katex-errors / broken-images, all 21 cells: `0 / 0 / 0` (home, lieb-robinson-locality, positivity-cstar-norms, spectral-gap-stability, lattice-eft-equivalence, bordism-realizability, synthesis all clean at all 3 widths). Console: 0 errors on every page; only benign Next.js font-preload warnings (`preloaded but not used within a few seconds`) on several paper pages — not a rendering defect, no action taken. **TOC checks (spectral-gap-stability):** scrolling to 50% of document height (scrollY 14402/28804) correctly highlighted `.toc-active` on "5.3 Theorem III-B: stability as condensed-openness" (`#sec:stability-thmB`), exactly matching the nearest heading above the viewport (verified independently via heading `getBoundingClientRect`); clicking the "9 Discussion" TOC link navigated the URL to `#sec:discussion`, scrolled the heading to just below the sticky header (~88px), and moved the active-TOC state to match — both PASS. **Landing page:** all 6 paper cards link to distinct, correct `/papers/<slug>/` hrefs; hero spine (`I_{d,G} → Ham_{d,G} → Gap_{d,G} → Phase^st_{d,G} → IP^cond_{d,G}`) renders as 5 `.katex` spans with 0 raw backslashes in body text. Visual spot-check screenshots (390px + 1280px per page, saved to `.playwright-mcp/qa-screenshots/`, not committed) confirm consistent dark theme, no overlapping text, no layout breaks. One infra hiccup: the `serve` background process was killed mid-run by an unrelated signal (exit code 7, "Gracefully shutting down") between the positivity and lattice-eft-equivalence pages — restarted with `disown` and testing resumed with no data loss. No source changes were needed in `website/app|components|globals.css`; `out/` is unchanged from the prior build. Server process killed at end of run.

**2026-07-14 — agent: HTML citations/cross-refs fixer (P6 follow-up)** — COMPLETE, all 3 hard gates PASS on `website/out/papers/*/index.html`. Fixed the two pandoc-pipeline gaps website-builder flagged (empty citations, unresolved `[eq:label]`-style cross-refs); nothing outside `scripts/`, `docs/papers/`, `website/` touched, `papers/latex/*.tex` and all PDFs byte-identical/untouched throughout (verified via checksum/mtime after the fact). **Citations:** `scripts/latex2html.py` now runs pandoc with `--citeproc --bibliography=papers/latex/references.bib --csl=scripts/numeric.csl`; wrote `scripts/numeric.csl`, a minimal self-authored numeric style (not derived from any third-party CSL) giving `[1]`/`[2,3]`-style inline cites and a numbered reference list, tested directly against every entry type in the shared bib (article/book/misc/incollection, incl. arXiv `eprint`/`archivePrefix` entries and the 6 internal cross-paper `paperX` keys). Corrected a premise in my brief: positivity-cstar-norms's embedded `\begin{thebibliography}` does **not** render natively — empirically it left every `\cite` span empty AND rendered the bibitems as an unnumbered, disconnected paragraph dump (even leaking the `{99}` capacity argument as stray text). Since all 27 of its `\cite` keys already exist in the shared `references.bib`, the fix strips the embedded thebibliography from the pandoc-input copy only and runs the same citeproc path as the other 5 papers. **Cross-references:** rather than hand-computing "kind word + aux number" (the label-prefix table breaks on real cases in this corpus — mixed-kind lists like `\Cref{prop:hullAF,thm:crossed}`, and cleveref's automatic range compression, e.g. a 5-item `\Cref` list on consecutive conjectures renders as "Conjectures II-1 to II-5", not an enumeration), `latex2html.py` now compiles a throwaway scratch copy of each paper (`--scratch-dir`, default a fresh temp dir; every unique `\Cref`/`\cref`/`\ref`/`\eqref` call gets appended as a dump appendix before `\end{document}`, never touching `papers/latex/` or its PDFs) and reads the literal cleveref-rendered text back out via `pdftotext`, so the substituted text is byte-for-byte whatever LaTeX actually produced — including the mixed-kind and range-compression cases above. A lighter `.aux`-`\newlabel` + label-prefix fallback (the originally-briefed approach) fires only if a specific harvest entry is missing; it was not needed anywhere in this run (0 fallback uses across all 6 papers). Also added a `References` heading (`<h1 class="unnumbered">`, matching pandoc's own unnumbered-section styling) before citeproc's heading-less `<div id="refs">`, for parity with the PDF. **Gate results (website/out/papers/*/index.html):** literal `[eq:`/`[thm:`/`[prop:`/`[sec:`/etc. leftovers = 0; empty `<span class="citation">` spans = 0; `katex-error` = 0 (also 0 `math-fallback` triggers, i.e. no new KaTeX regressions). **Spot-checks vs `pdftotext` of the real PDFs** (6, across 4 papers — exceeding the 3-refs/2-papers ask): spectral-gap-stability `eq:disp`→`(5)`, `sec:intro-wall`→"Section 1.2", 3-item `\Cref{thm:bhm,thm:mz,thm:nsy}`→"Theorems 5.5 to 5.7"; positivity-cstar-norms 5-item conjecture list→"Conjectures II-1 to II-5", mixed `\Cref{thm:aoki,cor:condinvariant}`→"Theorem II-B and corollary 6.1", mixed `\Cref{prop:hullAF,thm:crossed}`→"Proposition 7.1 and Theorem II-C"; lattice-eft-equivalence `tab:tenfold`→"Table 1"; bordism-realizability `\Cref{sec:cohomology,sec:beyond}`→"Sections 3 and 4" — all exact matches. Full regeneration of all 6 papers (`python3 scripts/latex2html.py --config scripts/papers-config.json`) + `cd website && npm run build` both ran clean, ~11s for the HTML regen. Known limitation: none outstanding at 0-count on all 3 gates; the only residual judgment call is that mixed-kind multi-ref capitalization/joining is whatever cleveref itself rendered (harvested verbatim) rather than a hand-specified rule, which is a strictly more faithful outcome than the originally-briefed prefix-table approach, not a gap.

**2026-07-14 — website-builder (P6 codex-review fixes) — DONE-FIXES, all 6 applied, rebuilt clean.** Applied all 6 findings from the mandatory codex website review (6d, NEEDS_FIX): (1) `lib/content.ts` DOMPurify config now `ADD_TAGS:['semantics','annotation']`, `ADD_ATTR:['style','encoding']` — the mathMl profile alone was dropping KaTeX's semantics/annotation wrapper on every formula; verified 470–700 `<semantics>` elements now present per paper page (was 0 in spirit, 3671/3676 broken per codex). (2) Body headings demoted one level in `lib/content.ts` (new `demoteHeadings`: h2→h3 first, then h1→h2, order matters so the freshly-made h2s aren't re-caught) so the paper title is the page's only `<h1>`; `Heading`/`TocHeading` types changed `1|2`→`2|3` across `lib/content.ts`/`components/TableOfContents.tsx`, CSS selectors `.paper-content > h1`/`h2` → `> h2`/`h3`. Verified: exactly 1 `<h1>` on all 7 pages (was 2 on every paper page). (3) ARIA: `nav aria-label="Primary"` (layout.tsx), TOC `aside aria-labelledby="toc-heading"` + stable `id="toc-heading"`/`id="toc-mobile-panel"` + `button aria-controls="toc-mobile-panel"` (also switched the mobile panel from conditional-mount to always-rendered-with-`hidden`, so the aria-controls target reliably exists in the DOM), `nav aria-label="Paper navigation"` on prev/next (page.tsx). (4) OG/canonical localhost: `layout.tsx`'s `siteUrl` fallback chain is now `NEXT_PUBLIC_SITE_URL → VERCEL_PROJECT_PRODUCTION_URL → hardcoded https://topological-phases-of-matter.vercel.app` (never localhost); added `openGraph.url:'/'` to homepage metadata. Grepped rebuilt `out/`: **zero** `localhost` in any HTML file (the one remaining repo-wide hit is inside Next's own vendored core-js URL-polyfill bundle — a literal `"localhost"===s.host` WHATWG-spec host-normalization check, unrelated to any page URL); every page's og:image/og:url/twitter:image/canonical now resolve to `https://topological-phases-of-matter.vercel.app/...`. (5) Contrast: `--text-dim` `#726f94`→`#8b88ad` (hand-computed WCAG relative luminance: ~5.98:1 on `--bg`, ~5.61:1 on `--surface`, both ≥4.5:1 with headroom); `.btn.primary:hover` text `#fff`→`var(--bg)` (near-black on the lighter `--accent-hover` fill, ~5.99:1, reusing the same token `.part-badge` already uses for text-on-accent) rather than darkening the hover fill, since the base `.btn.primary` state already passes at 4.81:1 and only the hover state was flagged. (6) Touch targets: `.btn` → `min-height:44px` + `justify-content:center`; new `.toc-mobile-panel .toc-list a` rule → `min-height:44px`, `display:flex;align-items:center` (inserted before the existing `.toc-sub` padding-left rule so the indent override still wins at tied specificity). Verified live via Playwright at 390px: all `.btn` elements measure exactly 44px height; TOC mobile-panel links measure 44–53px; hovered `.btn.primary` computed `color` is `rgb(6,6,13)` (`--bg`) confirmed. Rebuilt clean (`rm -rf out && npm run build`, 0 type/lint errors) picking up both these fixes and the citations/cross-refs fixer's regenerated `docs/papers/*.html` in the same pass (vendored copy confirmed byte-identical + newer via `scripts/copy-assets.mjs`'s prebuild hook — no stale content). Full regression re-check on all 6 papers + homepage: `katex-error`=0, `math-fallback`=0, raw `\(` delimiters=0, exactly 1 `<h1>` per page, 0 horizontal overflow at 390px (unchanged from before). Not deployed (per instruction) — ready for re-review/deploy.

**2026-07-14 — agent: browser-QA (P6 RE-VERIFY after codex-fix rebuild)** — COMPLETE, 0 defects, all checks PASS. Re-served the rebuilt `website/out/` (fresh `npm run build` from website-builder's 6-fix pass, confirmed by file mtime) via `npx serve out -l 4173`, re-ran the full 7-page × 3-width matrix (390/768/1280) with Playwright MCP. **Overflow/katex-errors/broken-images: 21/21 cells `0/0/0`** on home + all 6 papers. One methodology note: an initial broken-image reading of 5 (home, 390px) was a false positive from native `loading="lazy"` — the last grid card (`bordism-realizability.png`) sits ~4300px down the page and hadn't been fetched yet at check-time; confirmed via direct curl (HTTP 200), `file`/`sips` (valid uncorrupted 2550×3300 PNG), and a fresh in-page `Image()` load (succeeded in 3.6ms) that the file itself was never broken — switched to a scroll-through-then-check methodology (matches what full-page screenshots implicitly did in the first QA pass) and confirmed 0/0/0 thereafter on every page. **TOC (spectral-gap-stability):** scroll-spy at the true settled 50% scroll position (scrollY 14404.5/28809, using instant scroll + 700ms settle to avoid catching a smooth-scroll transient) still lands `.toc-active` exactly on "5.3 Theorem III-B" matching the nearest heading; click-nav on "9 Discussion" still updates the URL, scroll position (~88px below sticky header), and active state correctly. Both PASS, unchanged from first pass. **Landing page:** 6/6 card links correct, hero renders 5 `.katex` spans, 0 raw backslashes — unchanged. **New checks requested for re-verify, all 4 PASS:** (a) zero `localhost` in og:/twitter:/canonical tags across all 7 pages (static grep, confirmed by inspecting actual tag values — all resolve to `https://topological-phases-of-matter.vercel.app/...`); (b) tap-targets at 390px — all `.btn` elements 44px on every page (home has none, 6 paper pages have 3–4 each); mobile TOC panel links (had to click the actual `<button aria-controls="toc-mobile-panel">`, not the outer `.paper-toc-toggle` wrapper div, to expand it) measure 44–55.47px across all 23 links on lieb-robinson-locality, 0 under 44px; (c) exactly one `<h1>` per page, all 7 (static grep + live DOM count during the sweep, both agree); (d) `<semantics>` count spot-check: spectral-gap-stability 605, lieb-robinson-locality 700 (both ≫100; all 6 paper pages range 470–700, checked for completeness). Screenshots (14, 390px+1280px × 7 pages) saved to `/Users/mlong/.claude/jobs/8a6937e3/tmp/qa-screens/` per instruction, not committed. Housekeeping: moved all screenshots out of the repo root into the job-tmp qa-screens dir (twice — once after the first pass, once after this one), removed the `.playwright-mcp/` directory the Playwright tooling recreates in the project root each run, verified `git status --porcelain | grep -E 'png|playwright'` empty both times. No `website/` source touched (report-only per instruction). Server killed at end of run.

**2026-07-14 — hverify-realizability (Part V `bordism-realizability`, Haskell verification)** — COMPLETE. Created `Proofs.hs` (new module) with equational/deterministic checks citing Prop (Kitaev-chain Z2 invariant, paper:634-663) and Prop (Cluster-state string order, paper:493-537); wired into `Main.hs` and both `.cabal` stanzas. 3-round mandatory codex review (gpt-5.5 xhigh, all rounds real subprocess runs, no skip): round 1 NEEDS_FIX (3 High: Kitaev invariants computed outside the proposition's domain with no rejection of `Delta_p~=0`/discriminant points; Stabilizer type safety — unvalidated `single` indices silently returning identity, `Char`-based Pauli letters, `zipWith` truncation on mismatched Pauli lengths; `StringWindow` preconditions `a<b`/even `b-a`/valid endpoints unenforced — plus 5 Medium, 2 Low) — fixed with `isGapped`/`GappedKitaevParams`/`mkGapped`/`windingNumberG`/`majoranaNumberG`, an abstract `Pauli` + `PauliLetter` ADT + validated `single` + length-checked `multPauli`/`anticommutes`, a `mkStringWindow` smart constructor now exclusively consumed by `stringOp`/`stringOrder`/`stringOrderRotated`/`numX`, algebraic (trig-free) `d_z` endpoints in `majoranaNumber`, an exact closed-form quadratic `bulkGap` (was a 400-point grid estimate), a dynamically-bracketed `detectBoundary`, and QuickCheck bounded `>=2.14 && <2.19`; round 2 NEEDS_FIX (1 Medium: `phaseLabel` conflated "outside the winding-number's proposition-domain" with "physically gapless," mislabeling the genuinely-gapped `Delta=0, |mu|>2|t|` normal-insulator locus) — fixed by making `phaseLabel` check `bulkGap` directly, with `isGapped` kept unchanged as the narrower invariant-domain guard, plus a pinned regression check; round 3 NEEDS_FIX (1 High: the exact-quadratic `bulkGap` could round to a tiny negative value right at a `Delta_p=0` perfect-square root, e.g. `KitaevParams (sqrt 3) (sqrt 2) 0` giving `g(x*)~=-4.44e-16`, so `sqrt` produced `NaN`, silently failing the `phaseLabel` gapless check) — 3-round cap reached, fixed post-cap per pipeline policy with a `sqrt (max 0 (minimum candidates))` clamp plus a pinned reproducer check, independently reproduced pre-fix and confirmed post-fix via a standalone script. Final state: `-Wall -Wextra -Werror` clean (0 warnings), `cabal run`/`cabal test` both exit 0, 10/10 QuickCheck properties (up from 7), 23/23 deterministic/exhaustive checks (up from 0, since `Proofs.hs` is new this session), all safety fixes independently verified behaviorally (not just type-checked) via a standalone script exercising every error/rejection path, no flakiness across 13 repeated runs across the three rounds. Liquid Haskell not installed on this machine (`which liquid` → not found) — Phase 4 skipped per policy, noted, does not block the pipeline. Published demo numerics (Kitaev-sweep table, cluster string-order table) byte-identical throughout all three rounds; only the illustrative boundary-detection demo got more precise (not a published claim) as a side effect of the `detectBoundary` bracketing fix. Reviews: `reviews/bordism-realizability-haskell-codex-review-round-{1,2,3}.md` + canonical `reviews/bordism-realizability-haskell-codex-review.md` (all >500 bytes, header+findings+VERDICT+fix-applied). Build artifacts cleaned (`dist-newstyle` removed, source dir back to 5 `.hs` + `.cabal`, matching sibling topics).
**2026-07-14 — agent: social-posts (P7)** — COMPLETE. Generated 24 social post files (4 platforms × 6 papers: lieb-robinson-locality, positivity-cstar-norms, spectral-gap-stability, lattice-eft-equivalence, bordism-realizability, synthesis) to `posts/{twitter,linkedin,facebook,bluesky}/`, each with YAML frontmatter (platform, topic, title, url, status: draft, created). Content grounded in `.knowledge-base.md` + `coordination/abstracts/*.md`, one distinct angle per paper (uniform LR light cone; Aoki solidification bridge; CPW undecidability wall + stability; stacking/group-completion spectrum vs. Freed-Hopkins; Kapustin-Fidkowski chiral no-go; synthesis boxed sequence local interactions→condensed moduli stack→gapped substack→phase ∞-groupoid→invertible phase spectrum). Cross-verified the "72 QuickCheck / 198 deterministic / sorry-free Lean 4" figures against this board's own final per-topic numbers before using them (10+32+8+15+7=72 QuickCheck; 12+18+127+18+23=198 deterministic; 0 `sorry` across all 6 `lean/CondensedPhases/*.lean` files) rather than taking them on faith. Twitter (261-274/280 chars) and Bluesky (287-294/300 chars) counts verified programmatically per-file, not estimated; hashtag counts in spec range everywhere (Twitter 3-5, LinkedIn 5-8, Bluesky 2-3); LinkedIn word counts 204-303 (spec ~200-400). Facebook posts checked with a precision grep (bold/italic/heading/bullet patterns, correctly excluding hashtag lines and "C*-algebra" notation as non-violations): 0 real markdown found. 0 em/en-dashes in body prose (4 hits are the verbatim paper-title reproduction "Lieb–Robinson" in frontmatter `title:` only, matching the title as given); 0 curly quotes; 0 banned AI-tell vocabulary (delve/crucial/underscore/tapestry/testament/leverage/seamless/pivotal/robust-as-filler etc.). All links use the verified Vercel URL from `.vercel-url` (https://topological-phases-of-matter-pi.vercel.app) and the GitHub repo (github.com/YonedaAI/topological-phases-of-matter, included per instruction — goes live at pipeline end); every file's paper link matches its own slug, no cross-wiring. Files only — nothing posted to any platform.
